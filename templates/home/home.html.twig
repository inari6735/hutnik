{% extends 'base.html.twig' %}

{% block title %}Strona główna{% endblock %}

{% block body %}
    <link rel="stylesheet" href="{{ asset('css/cover.css') }}">
    <div class="d-flex vh-100 text-center text-white bg-dark">
        <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
            <header class="mb-auto">
                <div>
                    {% if app.user %}
                        <a style="font-size: 1.17em" href="{{ url('app_user') }}" class="float-md-start mb-0 pt-2 text-decoration-none text-white fw-bold">{{ app.user.getFirstName() }} {{ app.user.getLastName() }}</a>
                    {% endif %}

                    <nav class="nav nav-masthead justify-content-center float-md-end">
                        <a class="nav-link fw-bold py-1 px-0 active" aria-current="page" href="{{ url('app_home') }}">Home</a>
                        {% if is_granted('ROLE_ADMIN') %}
                            <a class="nav-link fw-bold py-1 px-0" href="{{ url('app_admin_email') }}">Dostęp</a>
                        {% endif %}
                        <a class="nav-link fw-bold py-1 px-0" href="#">Informacje</a>
                        <a class="nav-link fw-bold py-1 px-0" href="#">Kontakt</a>
                        <a class="nav-link fw-bold py-1 px-0" href="{{ path('app_logout') }}">Wyloguj się</a>
                    </nav>
                </div>
            </header>

            <main class="px-3">
                <h1>Uroczysty Bal Wydziałowy</h1>
                <p class="lead">Bal jest okazją do wspólnych spotkań i rozmów, ale przede wszystkim do wspólnej zabawy, która potrwa do samego rana. Piękne Panie, eleganccy Panowie, super muzyka, to tylko początek zapowiedzi tej noworocznej imprezy.</p>
                <p class="lead">
                    <a href="{{ url('app_table') }}" class="btn btn-lg btn-secondary fw-bold border-white bg-white">Zajmij miejsce</a>
                </p>
            </main>

            <footer class="mt-auto text-white-50">
                <p></p>
            </footer>
        </div>
    </div>
{% endblock %}
